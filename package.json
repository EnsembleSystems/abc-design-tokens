{
  "name": "abc-design-tokens",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "npm run build:tokens && npm run build:reset",
    "build:tokens": "node build.js && stylelint --fix dist/tokens.css && prettier --ignore-path /dev/null --write dist/tokens.css",
    "build:reset": "node reset.js && stylelint --fix dist/reset.css && prettier --ignore-path /dev/null --write dist/reset.css",
    "copy": "cp dist/tokens.css dist/reset.css ../abc-aem-sites/styles/"
  },
  "devDependencies": {
    "prettier": "^3.8.4",
    "style-dictionary": "^4.0.0",
    "stylelint": "^16.26.1",
    "stylelint-config-recess-order": "^7.7.0",
    "stylelint-config-standard": "^39.0.1"
  }
}
