{
  "name": "abc-design-tokens",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "npm run build:tokens && npm run build:global",
    "build:tokens": "node build.js && stylelint --fix dist/tokens.css && prettier --ignore-path /dev/null --write dist/tokens.css",
    "build:global": "node global.js && stylelint --fix dist/global.css && prettier --ignore-path /dev/null --write dist/global.css",
    "copy": "cp dist/tokens.css dist/global.css ../abc-aem-sites/styles/"
  },
  "devDependencies": {
    "prettier": "^3.8.4",
    "style-dictionary": "^4.0.0",
    "stylelint": "^16.26.1",
    "stylelint-config-recess-order": "^7.7.0",
    "stylelint-config-standard": "^39.0.1"
  }
}
